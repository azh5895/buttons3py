input.onButtonPressed(Button.A, function () {
    basic.showString("Previous Song")
})
input.onButtonPressed(Button.B, function () {
    if (true) {
        basic.showString("Next Song")
    } else {
        basic.showString("Current Song")
    }
})
basic.forever(function () {
	
})
